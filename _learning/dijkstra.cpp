// GOAL: find shortest path from (1) to F (7) of the graph at slide 70
#include <iostream>
#include <limits>
using namespace std;

const INF = numeric_limits<unsigned int>::max(); // now "inf" can simulate infinity because it's the max int representable 
struct node {
    char name;
    unsigned int 


void dijkstra (int graph, int source, int target) {

}

int main() {
    vector<pair<char, int>>

    return 0;
}
